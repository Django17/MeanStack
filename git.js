// git is version control tool
// command
// git init // initialize git in your folder (.git)
// git status // check current status of folder (weather files and folder are tracked or not)
// git add (file name) or .(for all file) // stage 
// git commit -m 'message_to_be_commited'
// if(remote is connected) (cloud servce provider has connection to lcoal projecct)
// git push origin master this will push changes in default master branch

// git config user.name 'aafno_name'
// git config user.email 'aafno_email'

// git log --> sees history of commit

// repeated action for daily use
// git add .
// git commit -m 'git commmit message'
// git push origin master
// git stash // ignore all the local changes

// cloining part
// git clone < remote url > this will make a copy of remote project folder into your local machine
// cd cloned folder directory to go into your cloned project

// repeated actions
// git pull origin master
